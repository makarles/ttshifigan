mode: resynthesize
out_dir: resynth_outputs
batch_size: 1
num_workers: 0
device: cuda
save_ref: true
suffix: _gen

ckpt:
  path: outputs_phase1/last.pt

dataset:
  root: data/ruslan
  audio_dir: audio
  transcriptions_dir: transcriptions
  sample_rate: 22050
  use_transcriptions: false

mel:
  sample_rate: 22050
  n_fft: 1024
  win_length: 1024
  hop_length: 256
  n_mels: 80
  f_min: 0
  f_max: 11025
  center: true
  power: 1.0
  mel_norm: null
